# Super+Ctrl+I — input remap TUI (keyd), Hyprland floating terminal
# Use absolute path: Hyprland/Quickshell PATH may omit ~/.local/bin.
unbind = Super+Ctrl, I
bind = Super+Ctrl, I, exec, kitty --class TUI.float -e $HOME/.local/bin/hyperwebster-input-remap
